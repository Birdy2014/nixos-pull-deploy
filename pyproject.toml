[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "nixos-pull-deploy"
version = "0.1.0"

[project.scripts]
nixos-pull-deploy = "nixos_pull_deploy.__main__:main"

[tool.setuptools.packages.find]
include = ["nixos_pull_deploy"]
